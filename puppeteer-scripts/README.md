# puppeteer-scripts

Puppeteer scripts to take screenshots of `.html` documents.

## static

Use `puppeteer` to take a screenshot of the visual included through `./static/index.html`.

```bash
# node script-static
npm run screenshot
```

Script saves the file locally as `./static/index.png`.
